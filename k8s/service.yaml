apiVersion: v1
kind: Service
metadata:
  name: users-api
  namespace: fcg
  labels:
    app: users-api
spec:
  selector:
    app: users-api
  ports:
    - name: http
      protocol: TCP
      port: 8080
      targetPort: 8080
  type: ClusterIP
